/**
 * 
 * same as fibs but solved recursively.
 * This can be done in just a couple of lines
 * (or 1 if you’re crazy,
 * but don’t consider either of these lengths a requirement…
 * just get it done).
 */
function fibsRec(){
    //todo:
}